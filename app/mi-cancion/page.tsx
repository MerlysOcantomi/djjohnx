import type {Metadata} from "next"
import {RequestForm} from "@/components/song-requests/request-form"
import {EnergyBar} from "@/components/song-requests/energy-bar"
import {getPublicRound} from "@/lib/song-requests/data"
import {getManualBizumConfig} from "@/lib/song-requests/config"
export const dynamic="force-dynamic"
export const metadata:Metadata={title:"Mi Canción",alternates:{canonical:"/mi-cancion"}}
export default async function Page(){const[round,config]=await Promise.all([getPublicRound().catch(()=>({status:"closed" as const,capacity:0,occupied:0,available:0,percent:0,message:"Mi Canción no está disponible temporalmente."})),Promise.resolve(getManualBizumConfig())]);const open=config.available&&["open","last_round"].includes(round.status)&&round.available>0;const minimum=new Intl.NumberFormat("es-ES",{style:"currency",currency:"EUR"}).format(config.minAmountCents/100);return <main className="min-h-dvh bg-[#08080b] px-4 py-6 text-white"><div className="mx-auto max-w-md space-y-5"><header className="text-center"><p className="font-black tracking-[.25em] text-primary">DJ JOHNX</p><h1 className="mt-2 text-4xl font-black">Mi Canción</h1><p className="mt-2 text-white/70">Activa tu pase y elige tus canciones</p></header><EnergyBar initial={round}/>{open?<section className="rounded-3xl border border-white/15 bg-white/[.06] p-5"><RequestForm available={round.available} minAmountCents={config.minAmountCents}/><p className="mt-4 text-xs leading-5 text-white/65">Aporta la cantidad que quieras desde {minimum}. Puedes activar entre una y cinco canciones, según los espacios disponibles. Cualquier cantidad adicional se considera un apoyo voluntario a DJ John y no aumenta el máximo de cinco canciones.</p></section>:<p className="rounded-2xl border border-amber-400/40 bg-amber-950 p-5 text-center font-bold">{config.available?round.message:"Mi Canción no está disponible temporalmente."}</p>}</div></main>}
